import { test, expect } from "vitest";

/**
 * This is a Sanity Check test.
 * Sanity Check tests are used to make sure that the test environment is working.
 */
test("sanity check", () => {
  expect(true).toBe(true);
});
